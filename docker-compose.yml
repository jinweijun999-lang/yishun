services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: fortune_app
    volumes:
      - fortune_app_postgres:/var/lib/postgresql/data

volumes:
  fortune_app_postgres:
