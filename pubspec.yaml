name: yishun_app
description: "YiShun 玄学出海App - 八字排盘与运势分析"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.0
  provider: ^6.1.1
  intl: ^0.20.2
  shared_preferences: ^2.2.2
  flutter_secure_storage: ^9.2.4
  cached_network_image: ^3.3.1
  shimmer: ^3.0.0
  fl_chart: ^0.69.0

  # Firebase
  firebase_core: ^3.13.0
  firebase_analytics: ^11.6.0
  firebase_crashlytics: ^4.3.10

  # Stripe
  flutter_stripe: ^12.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  generate: true
